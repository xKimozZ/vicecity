import styles from "./AudioMenu.module.css";

const AudioMenu = () => {

  return (
    <div className={`${styles.briefContainer} arborcrest arborcrestM`}>
      Coming soon...
      <br />
      Note: Map Menu is also still being worked on.
      <br/>
      Will finish that first, and Audio Menu will come last.
      <br/>
      After that, this project can be considered to have entered the Alpha phase (feature complete).
    </div>
  );
};

export default AudioMenu;
